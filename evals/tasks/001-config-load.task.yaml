---
id: 001-config-load
description: Worker can load central config and set seeds
type: unit
capability: config
---

# Eval: Config Load

## Input
- Fresh clone with config/config.yaml

## Execution
1. Run `python -c "from src.utils import load_config; print(load_config())"`

## Verification
- Returns dict with keys: data, model, training, paths
- No exceptions

## Grader
code-based: evals/graders/code_based.py::test_config_load
